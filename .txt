
model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  password  String
  role      Role     @default(ADMIN)
  station   Station?   // ✅ sirf aik station (optional)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
